# AITask 本地运行时改造说明：Agent Mailbox、Worker 与 OpenViking 记忆检索

> 面向 Codex 的实施文档  
> 目标：在不破坏现有 `aitask-watch` 定位的前提下，扩展 AITask 的本地 Agent 协作能力，让系统支持 `@自己的消息`、`全局消息`、`最新消息`、OpenViking 记忆同步与可选 Agent 自动唤醒。

---

## 1. 背景与当前真实状态

当前 `aitask-watch` 的设计定位是：

> 跨多 AI Agent（Claude Code / Codex CLI / Gemini CLI）的统一事件管道守护进程，把 `aitask events` 这条 NDJSON 流接入每个编码 Agent 的会话生命周期。

现有链路大致如下：

```text
服务端 actionable events
  mention / task_delegated
        ↓
aitask events 通过 WebSocket 订阅项目事件流
        ↓
写入 ~/.aitask/events.ndjson
        ↓
aitask-watch 发系统通知 / 写文件
        ↓
Claude Code / Codex CLI / Gemini CLI 通过各自 hook 获取上下文
```

现有设计的核心是：

```text
一个 daemon 喂 N 个 Agent
```

即：

```text
aitask-watch
  ↓
events.ndjson
  ↓
各 Agent 的 SessionStart / per-turn hook
  ↓
事件被投影进 Agent 当前会话上下文
```

这个设计解决的是：

```text
让正在启动或正在交互中的 Agent 能看到外部异步事件。
```

它不解决：

```text
Agent idle 后被 mention 自动唤醒。
Agent 拥有自己的 inbox。
Agent 对事件做 ack / handled / failed / retry。
每轮消息自动同步到 OpenViking。
Agent 基于历史记忆做语义检索和总结召回。
```

因此，当前的 `aitask-watch` 更准确的定位应该是：

> 本地事件流采集与会话上下文注入器，而不是完整的 Agent 自动协作运行时。

---

## 2. 本次改造的核心目标

本次不要推翻原有设计，而是在原有 `aitask-watch` 之上新增几个清晰分层：

```text
aitask-watch
  = 事件流采集与 hook 上下文注入器

aitask-inbox / 本地索引
  = Agent 邮箱、全局消息、最新消息、状态管理

aitask-worker
  = 后台索引、OpenViking 同步、总结生成

OpenViking
  = 长期记忆、语义检索、总结召回

aitask watch --agent <name>
  = 以 Agent 身份消费 inbox，并可选唤醒对应 Agent 执行
```

最终目标链路：

```text
Agent A 发起 mention / task
        ↓
aitask 记录结构化事件
        ↓
aitask-watch 订阅服务端事件并写入 events.ndjson
        ↓
aitask-worker 消费 events.ndjson
        ↓
写入本地 index / inbox / 状态库
        ↓
同步事件、对话、结果、摘要到 OpenViking
        ↓
Agent B 的 watch 以自身身份获取 @自己的消息
        ↓
查询 OpenViking 获取相关上下文
        ↓
可选唤醒 Claude Code / Codex / Gemini 执行
        ↓
回写结果，标记 handled / failed / skipped
        ↓
worker 再同步结果到 OpenViking
```

一句话总结：

> `aitask` 管“谁发了什么、谁该处理、处理到哪了”；`OpenViking` 管“过去发生过什么、现在应该想起什么”。

---

## 3. 不能改变的既有定位

请不要把现有 `aitask-watch` 改成一个巨大的混合进程。

`aitask-watch` 应继续负责：

1. 通过 `aitask events` / WebSocket 订阅服务端项目事件流。
2. 将事件追加写入 `~/.aitask/events.ndjson`。
3. 发系统通知。
4. 维持原有 tmux daemon 运行方式。
5. 继续支持 SessionStart / per-turn hook 的 context injection。
6. 为 Claude Code、Codex CLI、Gemini CLI 的现有 hook 提供事件来源。

`aitask-watch` 不应该负责：

1. OpenViking 同步。
2. 长期记忆检索。
3. 复杂任务状态管理。
4. Agent 自动唤醒。
5. 事件处理结果判断。
6. summary 生成。
7. retry 调度。

建议文档中把 `aitask-watch` 的定位修正为：

```text
aitask-watch 是 AITask 本地事件流守护进程，负责将服务端 actionable 事件流订阅到本地 NDJSON，并为各类 Agent 集成提供统一事件来源。

在默认 Context Injection 模式下，aitask-watch 通过 SessionStart / per-turn hook 将事件注入 Claude Code、Codex CLI、Gemini CLI 的会话上下文，补齐它们对外部异步事件的感知能力。
```

---

## 4. 新增两种运行模式的文档定义

需要明确区分两种模式。

### 4.1 Context Injection Mode

这是现有模式。

适用场景：

```text
人类正在使用 Agent。
Agent 启动时需要知道最近事件。
Agent turn 前后需要拿到增量事件。
不要求 idle Agent 自动响应。
```

链路：

```text
aitask-watch
  ↓
~/.aitask/events.ndjson
  ↓
Claude / Codex / Gemini hooks
  ↓
additional context / prompt context
```

特点：

```text
被动注入上下文。
依赖 Agent 自身生命周期。
不能保证实时自动回复。
```

### 4.2 Mailbox Worker Mode

这是新增模式。

适用场景：

```text
Agent 需要拥有自己的 inbox。
需要查询 @自己的消息、全局消息、最新消息。
需要 ack / handled / failed / skipped / retry。
需要 OpenViking 记忆同步。
需要可选自动唤醒 Agent。
```

链路：

```text
aitask-watch
  ↓
events.ndjson
  ↓
aitask-worker
  ↓
SQLite / state index
  ↓
OpenViking memory
  ↓
aitask inbox / latest / summary / context
  ↓
aitask watch --agent <name> --wake
```

特点：

```text
主动消费消息。
有结构化状态。
可异步同步 OpenViking。
可扩展到自动唤醒。
```

---

## 5. 消息分类需求

新增的消息查询能力至少支持四类。

### 5.1 @ 自己的消息

命令示例：

```bash
aitask inbox --agent claude-code
```

语义：

```text
获取发给 claude-code 的 mention / task_delegated / actionable events。
```

过滤条件建议：

```text
event.kind in ["mention", "task_delegated"]
AND event.to == current_agent
AND event.from != current_agent
AND event.status not in ["handled", "skipped"]
```

这类消息可以触发自动处理。

### 5.2 全局消息

命令示例：

```bash
aitask inbox --global
```

语义：

```text
获取项目级别、广播级别、所有 Agent 都可以看的消息。
```

典型内容：

```text
项目状态变化
关键决策
任务完成通知
全局上下文更新
Agent 发布的总结
```

注意：

```text
全局消息默认可读，但不默认唤醒所有 Agent。
只有事件显式设置 wake=true 时，才允许触发自动唤醒。
```

### 5.3 最新消息

命令示例：

```bash
aitask latest --limit 20
```

语义：

```text
获取最近发生的事件，不管是不是发给自己。
```

用途：

```text
SessionStart hook 注入最近项目动态。
新会话冷启动时快速恢复项目上下文。
人类查看最近活动。
```

### 5.4 总结与记忆消息

命令示例：

```bash
aitask summary --project aitask
aitask summary --agent claude-code
aitask summary --thread <thread_id>
aitask search "wake-and-inject 方案"
aitask context --event <event_id>
```

语义：

```text
从 OpenViking 中获取历史记忆、相关上下文、项目摘要、线程摘要、Agent 行为摘要。
```

---

## 6. OpenViking 的职责边界

OpenViking 的定位必须写清楚：

```text
OpenViking 是记忆、检索、总结、上下文召回系统。
OpenViking 不是事件总线。
OpenViking 不是消息队列。
OpenViking 不负责调度。
OpenViking 不负责唤醒 Agent。
OpenViking 不负责 ack / handled / retry / cursor 等强状态。
```

OpenViking 负责：

```text
长期记忆
语义检索
历史上下文召回
thread summary
project summary
agent summary
相关任务召回
```

AITask 本地状态负责：

```text
event_id
from / to / scope
thread_id
project
created_at
seen / unread
acked
handled
failed
skipped
retry_count
cursor
sync_status
summary_status
```

关键原则：

```text
状态类数据不要依赖 OpenViking。
语义类数据交给 OpenViking。
```

---

## 7. 本地状态层设计

建议新增一个本地状态库：

```text
~/.aitask/state.db
```

或者：

```text
~/.aitask/index.sqlite
```

该库用于从 `events.ndjson` 建立结构化索引。

### 7.1 建议表结构

#### events

保存规范化后的事件。

字段建议：

```text
id TEXT PRIMARY KEY
kind TEXT
scope TEXT
project TEXT
thread_id TEXT
from_agent TEXT
to_agent TEXT
body TEXT
raw_json TEXT
created_at TEXT
indexed_at TEXT
```

#### agent_inbox

保存 Agent 维度的收件箱。

```text
id INTEGER PRIMARY KEY AUTOINCREMENT
event_id TEXT
agent TEXT
status TEXT
seen_at TEXT
acked_at TEXT
handled_at TEXT
failed_at TEXT
retry_count INTEGER
last_error TEXT
```

`status` 可选：

```text
unread
seen
acked
handled
failed
skipped
```

#### global_feed

保存全局消息。

```text
id INTEGER PRIMARY KEY AUTOINCREMENT
event_id TEXT
project TEXT
visibility TEXT
wake INTEGER
created_at TEXT
```

#### cursors

保存不同消费者的 cursor。

```text
consumer TEXT PRIMARY KEY
source TEXT
offset INTEGER
event_id TEXT
updated_at TEXT
```

示例消费者：

```text
worker:indexer
worker:openviking
worker:summarizer
hook:claude-code
hook:codex
hook:gemini
agent-watch:claude-code
agent-watch:codex
agent-watch:gemini
```

#### memory_sync

保存 OpenViking 同步状态。

```text
event_id TEXT PRIMARY KEY
status TEXT
synced_at TEXT
retry_count INTEGER
last_error TEXT
openviking_id TEXT
```

#### summaries

保存 summary cache。

```text
id TEXT PRIMARY KEY
scope TEXT
scope_id TEXT
summary TEXT
source_event_id TEXT
updated_at TEXT
memory_id TEXT
```

---

## 8. 事件结构建议

现有事件可能已经有部分字段。需要尽量规范化为如下结构：

```json
{
  "id": "evt_123",
  "kind": "mention",
  "scope": "agent",
  "project": "aitask",
  "thread_id": "thr_456",
  "from": "codex",
  "to": "claude-code",
  "body": "@claude-code 请检查 wake-and-inject 方案",
  "wake": false,
  "created_at": "2026-05-07T14:00:00Z",
  "metadata": {}
}
```

字段说明：

```text
id         全局唯一事件 ID
kind       mention / task_delegated / broadcast / task_done / summary / note
scope      agent / global / thread / project
project    项目名
thread_id  线程 ID
from       发送方 Agent
to         目标 Agent，可为空
body       事件正文
wake       是否允许触发自动唤醒
created_at 创建时间
metadata   扩展字段
```

注意：

```text
调度不要只依赖 body 里的 @xxx 文本。
应该优先依赖结构化字段 to / scope / kind。
```

---

## 9. 新增 CLI 设计

### 9.1 消息查询类

```bash
aitask inbox --agent claude-code
aitask inbox --global
aitask latest --limit 20
aitask thread <thread_id>
```

### 9.2 状态更新类

```bash
aitask ack <event_id> --agent claude-code
aitask done <event_id> --agent claude-code
aitask skip <event_id> --agent claude-code --reason "not actionable"
aitask fail <event_id> --agent claude-code --error "..."
```

### 9.3 记忆检索类

```bash
aitask search "wake-and-inject 方案"
aitask context --event <event_id>
aitask context --thread <thread_id>
aitask summary --project aitask
aitask summary --agent claude-code
aitask summary --thread <thread_id>
```

### 9.4 后台 worker 类

```bash
aitask worker --memory openviking
aitask worker --once
aitask worker --daemon
aitask sync --memory openviking
```

### 9.5 Agent 消费类

```bash
aitask watch --agent claude-code --wake
aitask watch --agent claude-code --exec ./handlers/claude-code.sh
aitask watch --agent codex --wake
aitask watch --agent gemini --wake
```

---

## 10. `aitask-worker` 设计

新增 `aitask-worker`，职责是：

```text
消费 events.ndjson
规范化事件
写入 SQLite / state.db
分类为 agent inbox / global feed / latest feed
同步到 OpenViking
生成 summary
记录 cursor / sync status / retry
```

### 10.1 worker 流程

```text
1. ingest
   从 events.ndjson 读取新行。

2. normalize
   规范化字段，补全 id / kind / scope / project / thread_id / from / to。

3. route
   根据 kind / scope / to 分类：
   - agent inbox
   - global feed
   - latest feed
   - thread event

4. index
   写入 SQLite / state.db。

5. sync
   把有价值的事件同步到 OpenViking。

6. summarize
   按 thread / project / agent 生成或更新摘要。

7. mark
   更新 cursor / memory_sync / summaries 状态。
```

### 10.2 worker 不能做的事

```text
不能阻塞 events.ndjson 写入。
不能直接替代 aitask-watch 订阅 WebSocket。
不能承担 Agent 唤醒职责。
不能把 OpenViking 当成状态数据库。
```

### 10.3 同步到 OpenViking 的内容

应该同步：

```text
mention 正文
task_delegated 任务描述
Agent 回复
任务完成结果
失败原因
thread summary
project summary
agent summary
关键决策
重要上下文变化
```

不建议同步：

```text
心跳
daemon 启停
重复通知
低价值 debug log
超长工具原始输出
无意义 stdout
```

---

## 11. `aitask watch --agent` 设计

这是新增的 Agent 身份化消费者。

职责：

```text
以某个 Agent 身份查询 inbox。
只处理发给自己的未处理消息。
避免处理自己发出的消息。
查询 OpenViking 获取相关上下文。
组装 prompt。
可选调用 claude / codex / gemini runner。
回写结果。
标记 handled / failed / skipped。
```

### 11.1 运行示例

```bash
aitask watch --agent claude-code --exec ./handlers/claude-code.sh
```

或：

```bash
aitask watch --agent claude-code --wake
```

### 11.2 处理流程

```text
1. 获取当前 agent 的未处理 inbox。
2. 按 created_at 排序。
3. 对每个 event 加锁，避免重复处理。
4. ack event。
5. 调用 aitask context --event <event_id> 获取 OpenViking 相关上下文。
6. 渲染 prompt。
7. 调用对应 runner。
8. 捕获 stdout / stderr / exit code。
9. 成功则 aitask done。
10. 失败则 aitask fail，并记录 retry_count / last_error。
```

### 11.3 不建议第一版做 stdin 注入

不要优先做：

```text
tmux send-keys 注入当前活跃 REPL
往未知状态的 stdin 写入多行 prompt
试图接管正在生成中的 Agent 会话
```

第一版更建议做：

```text
事件触发一次性 Agent worker。
例如 claude -p / codex exec / gemini cli 一次性处理。
```

原因：

```text
REPL 状态不稳定。
工具确认状态不可控。
多行输入容易破坏当前会话。
不同 CLI 行为不一致。
一次性 runner 更可控、可重试、可记录。
```

---

## 12. Prompt 渲染建议

需要新增统一 prompt renderer，而不是每个 handler 自己拼。

命令示例：

```bash
aitask render-prompt --event <event_id> --agent claude-code
```

输出结构示例：

```text
You are claude-code in an AITask multi-agent collaboration system.

You received an actionable event.

Event:
- ID: evt_123
- Kind: mention
- From: codex
- To: claude-code
- Project: aitask
- Thread: thr_456
- Created At: 2026-05-07T14:00:00Z

Task:
@claude-code 请检查 wake-and-inject 方案。

Relevant Context:
<OpenViking recalled context here>

Instructions:
1. Decide whether this event is addressed to you.
2. If actionable, complete the task.
3. Reply through AITask.
4. Mark the event done, skipped, or failed.
5. Do not process events sent by yourself.
```

---

## 13. 组件目录与 Skill 文件规范

用户明确要求：

> 不是根目录集中放 `skills/xxx/SKILL.md`，而是每个组件目录自己带 `skills/SKILL.md`。

推荐目录形态：

```text
aitask/
  README.md
  docs/
    architecture.md
    modes.md
    openviking.md

  cli/
    internal/
    cmd/

  aitask-watch/
    README.md
    skills/
      SKILL.md
    internal/
      watcher/
      hooks/
      notify/

  aitask-worker/
    README.md
    skills/
      SKILL.md
    internal/
      indexer/
      openviking/
      summarizer/
      cursor/

  aitask-inbox/
    README.md
    skills/
      SKILL.md
    internal/
      inbox/
      status/
      query/

  aitask-agent-watch/
    README.md
    skills/
      SKILL.md
    internal/
      runner/
      prompt/
      wake/
      handlers/

  integrations/
    claude-code/
      skills/
        SKILL.md
      hooks/

    codex-cli/
      skills/
        SKILL.md
      hooks/

    gemini-cli/
      skills/
        SKILL.md
      hooks/
```

如果仓库不适合拆成多个顶层目录，也可以按现有结构放在功能模块目录下，但必须遵守：

```text
每个组件自己的 skills/SKILL.md 跟着该组件实现走。
不要用一个全局 skill 替代所有组件 skill。
```

---

## 14. 每个 Skill 文件必须包含的结构

每个 `skills/SKILL.md` 建议统一格式：

```markdown
# Skill: <component-name>

## 定位

## 负责什么

## 不负责什么

## 输入

## 输出

## 核心命令

## 状态文件

## 与其他组件的关系

## 常见流程

## 失败与重试策略

## Agent 使用注意事项
```

---

## 15. 各组件 Skill 文件内容要求

### 15.1 `aitask-watch/skills/SKILL.md`

必须写清：

```text
定位：
事件流采集与会话上下文注入器。

负责：
- 订阅服务端 events。
- 写入 ~/.aitask/events.ndjson。
- 发通知。
- 被 SessionStart hook 拉起。
- 给 Claude / Codex / Gemini hook 提供事件来源。

不负责：
- OpenViking 同步。
- inbox 状态。
- ack / handled / retry。
- Agent 自动唤醒。
- 总结生成。
```

### 15.2 `aitask-worker/skills/SKILL.md`

必须写清：

```text
定位：
后台索引、同步、总结 worker。

负责：
- 消费 events.ndjson。
- 写入 SQLite / state.db。
- 建立 inbox / global / latest 索引。
- 同步 OpenViking。
- 生成 thread / project / agent summary。
- 管理 sync cursor 和 retry。

不负责：
- WebSocket 订阅。
- 原始事件采集。
- Agent 唤醒。
- 判断任务是否应该执行。
```

### 15.3 `aitask-inbox/skills/SKILL.md`

必须写清：

```text
定位：
Agent 邮箱与消息查询。

负责：
- @自己的消息。
- 全局消息。
- 最新消息。
- 未读 / 已读。
- acked / handled / failed / skipped。
- agent cursor。
- thread_id / event_id 查询。

不负责：
- 语义检索。
- 长期记忆。
- OpenViking 存储。
- Agent 唤醒。
```

### 15.4 `aitask-agent-watch/skills/SKILL.md`

必须写清：

```text
定位：
以 Agent 身份消费 inbox，并可选唤醒执行。

负责：
- 以 --agent <name> 身份拉取 inbox。
- 只处理 @自己的消息。
- 避免处理自己发出的消息。
- 查询 OpenViking 上下文。
- 组装 prompt。
- 调用 claude / codex / gemini runner。
- 回写 done / fail / skip。

不负责：
- 原始事件采集。
- WebSocket 订阅。
- OpenViking 长期存储。
- 全局状态数据库。
```

### 15.5 `openviking-memory/skills/SKILL.md` 或 `integrations/openviking/skills/SKILL.md`

必须写清：

```text
定位：
OpenViking 记忆、检索、总结召回。

负责：
- 写入有价值事件和任务结果。
- 语义检索。
- 根据 event / thread / project 召回上下文。
- 生成和读取 summary。

不负责：
- 事件路由。
- 消息队列。
- ack / handled / retry。
- cursor。
- 唤醒 Agent。
```

### 15.6 Agent 集成 Skill

例如：

```text
integrations/claude-code/skills/SKILL.md
integrations/codex-cli/skills/SKILL.md
integrations/gemini-cli/skills/SKILL.md
```

需要写清每家 Agent 的 hook 能力差异：

```text
Claude Code:
- SessionStart 可注入。
- 没有等价 per-turn hook。
- 进行中只能通过 Monitor / tail 或 agent-watch runner 处理。

Codex CLI:
- 可通过 UserPromptSubmit / BeforeAgent 做增量注入。
- 可接入 inbox context。

Gemini CLI:
- 可通过对应 hook 做增量注入。
- 可接入 inbox context。
```

---

## 16. 文档结构建议

需要新增或更新文档：

```text
docs/architecture.md
docs/modes.md
docs/openviking.md
docs/local-runtime.md
```

### 16.1 `docs/architecture.md`

说明整体架构：

```text
aitask-watch
aitask-worker
aitask-inbox
aitask-agent-watch
OpenViking
Agent integrations
```

### 16.2 `docs/modes.md`

说明两种模式：

```text
Context Injection Mode
Mailbox Worker Mode
```

### 16.3 `docs/openviking.md`

说明 OpenViking 边界：

```text
只做记忆、检索、总结、上下文召回。
不做调度、状态、唤醒。
```

### 16.4 `docs/local-runtime.md`

说明本地运行时：

```text
events.ndjson
state.db
worker daemon
tmux watch
agent watch
OpenViking sync
```

---

## 17. 迁移实施顺序

请按阶段实现，不要一次性硬改所有逻辑。

### Phase 1：文档和定位修正

目标：

```text
明确 aitask-watch 不是自动唤醒器。
明确现有模式是 Context Injection Mode。
新增 Mailbox Worker Mode 设计文档。
明确 OpenViking 只做记忆和检索。
```

任务：

```text
1. 更新 aitask-watch 文档。
2. 新增 docs/modes.md。
3. 新增 docs/openviking.md。
4. 新增各组件 skills/SKILL.md 草案。
```

验收：

```text
文档中不再把 aitask-watch 描述为完整自动应答系统。
新旧模式边界清楚。
每个组件有自己的 skills/SKILL.md。
```

### Phase 2：本地索引与 inbox 查询

目标：

```text
把 events.ndjson 从纯日志升级为可查询 inbox。
```

任务：

```text
1. 新增 state.db / index.sqlite。
2. 新增 events / agent_inbox / global_feed / cursors 表。
3. 新增 aitask inbox --agent <name>。
4. 新增 aitask inbox --global。
5. 新增 aitask latest --limit N。
6. 新增 ack / done / fail / skip 状态命令。
```

验收：

```text
能查询 @自己的消息。
能查询全局消息。
能查询最新消息。
同一事件不会被重复处理。
状态可持久化。
```

### Phase 3：aitask-worker 与 OpenViking 同步

目标：

```text
后台 worker 自动消费 events.ndjson，并同步到 OpenViking。
```

任务：

```text
1. 实现 aitask worker --once。
2. 实现 aitask worker --daemon。
3. 实现 memory_sync 状态。
4. 实现 OpenViking 写入适配。
5. 实现基础 summary 生成或 summary cache。
6. 实现 aitask search / context / summary 命令。
```

验收：

```text
events.ndjson 新事件能被 worker 索引。
有价值事件能同步到 OpenViking。
OpenViking 挂掉不影响 events.ndjson 写入。
失败可重试。
summary / search / context 可用。
```

### Phase 4：Agent 身份化消费与可选唤醒

目标：

```text
让每个 Agent 以自身身份消费 inbox。
```

任务：

```text
1. 实现 aitask watch --agent <name>。
2. 实现 --exec handler。
3. 实现 prompt renderer。
4. 实现 event lock，避免重复处理。
5. 实现 done / fail / skip 回写。
6. 可选实现 --wake claude / codex / gemini。
```

验收：

```text
Codex @claude-code 后，只有 claude-code watcher 消费。
不会处理自己发出的消息。
成功处理后标记 handled。
失败后记录 failed 和 retry_count。
可从 OpenViking 拉取相关上下文。
```

---

## 18. 关键验收场景

### 场景 1：查询 @自己的消息

输入：

```text
Codex 发出 @claude-code 的 mention。
```

期望：

```bash
aitask inbox --agent claude-code
```

能看到该消息。

```bash
aitask inbox --agent gemini
```

不能看到该消息，除非该消息是 global 或明确发给 gemini。

### 场景 2：全局消息

输入：

```text
某 Agent 发布 project summary / broadcast。
```

期望：

```bash
aitask inbox --global
```

能看到该消息。

但默认不自动唤醒所有 Agent。

### 场景 3：最新消息

输入：

```text
系统有多条新事件。
```

期望：

```bash
aitask latest --limit 10
```

按时间返回最近事件。

### 场景 4：OpenViking 同步

输入：

```text
events.ndjson 新增 mention / task result。
```

期望：

```text
aitask-worker 自动同步到 OpenViking。
aitask search 能语义检索到相关内容。
aitask context --event 能召回相关上下文。
```

### 场景 5：Agent 自动消费

输入：

```text
Codex @claude-code 委托任务。
```

运行：

```bash
aitask watch --agent claude-code --exec ./handlers/claude-code.sh
```

期望：

```text
claude-code watcher 获取该消息。
查询 OpenViking 上下文。
调用 handler。
处理成功后标记 handled。
结果回写 aitask。
```

---

## 19. 注意事项与设计约束

1. 不要让 `aitask-watch` 变成万能进程。
2. 不要把 OpenViking 当状态数据库。
3. 不要只靠 `@xxx` 文本解析做调度，要使用结构化 `to` 字段。
4. 不要用全局 offset 替代 per-agent 状态。
5. 不要让全局消息默认唤醒所有 Agent。
6. 不要第一版优先做 stdin 注入当前 REPL。
7. 要保留原有 hook 注入能力。
8. 要保证 OpenViking 同步失败不影响事件采集。
9. 要保证 worker 可重启、可恢复、可重放。
10. 要保证每个组件有自己的 `skills/SKILL.md`。

---

## 20. 最终文档定位文本

可以在 README 或 architecture 中使用以下表述：

```text
AITask local runtime 由多个本地组件组成：

aitask-watch 负责订阅服务端事件流、写入本地 NDJSON，并为各 Agent 的 hook 注入提供事件来源。

aitask-worker 负责消费本地事件流，建立 Agent inbox / global feed / latest feed 的结构化索引，并将有价值的事件、任务结果和摘要异步同步到 OpenViking。

OpenViking 作为记忆和检索后端，只负责长期记忆、语义检索、总结和上下文召回，不承担事件路由、任务状态或 Agent 唤醒职责。

aitask watch --agent <name> 以指定 Agent 身份消费 inbox，可选调用对应 runner 完成自动处理，并将结果回写 AITask。
```

---

## 21. 最小可行版本清单

MVP 只需要先完成：

```text
1. 保留原 aitask-watch。
2. 新增/修正文档，明确 Context Injection Mode 与 Mailbox Worker Mode。
3. 新增每个组件自己的 skills/SKILL.md。
4. 实现 aitask inbox --agent <name>。
5. 实现 aitask inbox --global。
6. 实现 aitask latest。
7. 实现 aitask worker --memory openviking --once。
8. 实现 OpenViking 同步适配的最小版本。
9. 实现 aitask search / context 的最小版本。
10. 实现 aitask watch --agent <name> --exec 的最小版本。
```

MVP 不要求：

```text
1. 复杂 UI。
2. 完整跨机器调度。
3. stdin 注入正在运行的 REPL。
4. 完整任务编排系统。
5. 高级 summary 策略。
6. 多租户权限模型。
```

---

## 22. 给 Codex 的执行建议

请先做代码与文档盘点：

```text
1. 找到 aitask events 写 events.ndjson 的实现。
2. 找到 aitask-watch daemon / tmux 启动逻辑。
3. 找到 scripts/aitask-hooks/lib.sh。
4. 找到 Claude / Codex / Gemini hook 注入逻辑。
5. 确认现有事件 schema。
6. 确认是否已有 event_id / thread_id / from / to / project 字段。
7. 确认是否已有 reply / complete / ack 相关命令。
8. 确认当前本地状态目录结构。
```

然后按 Phase 1 → Phase 4 分阶段提交。

每个阶段都需要：

```text
1. 更新对应组件 README。
2. 更新对应组件 skills/SKILL.md。
3. 添加必要测试。
4. 保证旧的 Context Injection Mode 不被破坏。
```

---

## 23. 最终判断

这次改造不是要否定原 `aitask-watch`，而是要把原系统从：

```text
事件流落盘 + hook 上下文注入
```

扩展为：

```text
事件流落盘 + Agent inbox + 全局消息 + 最新消息 + OpenViking 记忆检索 + 可选 Agent 自动唤醒
```

关键是分层：

```text
aitask-watch：收事件，供 hook 注入。
aitask-inbox：管消息和状态。
aitask-worker：后台索引、同步、总结。
OpenViking：记忆、检索、上下文召回。
aitask watch --agent：以 Agent 身份消费消息并可选执行。
```

每个组件都必须有自己的：

```text
<component>/skills/SKILL.md
```

这样 Agent 在修改某个模块时，只需要读取该模块自己的 skill，就能理解职责、边界、输入输出和禁区。
